# Agents Bucket

## Scope
Use this bucket for agent workflow, tooling, and instruction-file notes.

## Summary
- This repo already references agentic development in `README.md`.
- The user-level OpenCode config enables the `superpowers` plugin and multiple agent-oriented models.
- Repo-local Multi Brain bootstraps shared memory without changing the user OpenCode profile.

## Open Threads
- Decide later whether to add more topic buckets such as `ui-refactor`, `backend`, or `deployment`.
